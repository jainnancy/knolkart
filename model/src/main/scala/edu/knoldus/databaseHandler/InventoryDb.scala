/*
package main.scala.edu.knoldus.databaseHandler

import main.scala.edu.knoldus.database.{Inventory, Item}

import scala.collection.mutable.ListBuffer

class InventoryDb {

  val inventoryList = new ListBuffer[Inventory]
  //val itemList = (new ItemDb).itemList
  def addInventory(itemList: ListBuffer[Item]): ListBuffer[Inventory] = {

    inventoryList += Inventory(itemList)
    inventoryList
  }
}
*/
